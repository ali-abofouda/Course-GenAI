'''
This is the main module for the package.
init means "initialize" and is used to initialize the package when it is imported. It can contain code that sets up the package, such as importing submodules or defining variables and functions that should be available when the package is imported. In this case, we will import all functions and variables from the math module directly into the current namespace, allowing us to use them without the math. prefix.
'''

